<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/style/output.css" rel="stylesheet">
    <title>Home Page</title>

</head>


<body>


    <div class="main min-h-screen w-full bg-gray-900 py-10 px-4 text-gray-100">
        <h1 class="font-bold mb-4 text-amber-400 text-center text-4xl">
            Task Manager
        </h1>


        <div class="form max-w-xl w-full bg-gray-800 mx-auto p-6 rounded-lg shadow-lg">
            <form action="/create" method="post" class="space-y-6">
                <div>
                    <label for="title" class="block text-sm font-medium text-gray-300 mb-1">Title</label>
                    <input type="text" id="title" name="title"
                        class="w-full px-4 py-2 bg-gray-700 border border-gray-600 text-white rounded focus:outline-none focus:ring-2 focus:ring-blue-500" />
                </div>

                <div>
                    <label for="message" class="block text-sm font-medium text-gray-300 mb-1">Message</label>
                    <input type="text" id="message" placeholder="Type your message..." name="content"
                        class="w-full h-32 px-4 py-3 bg-gray-700 border border-gray-600 text-white rounded focus:outline-none focus:ring-2 focus:ring-amber-400" />
                </div>


                <button type="submit"
                    class="w-full bg-amber-500 text-gray-900 font-semibold py-2 rounded hover:bg-amber-600 transition">
                    Create Task
                </button>

            </form>
        </div>



        <!-- Card Container -->
        <div class="px-4 py-10 bg-gray-900 text-gray-100">
            <div class="max-w-6xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                <% if (files.length > 0) { %>
                <% files.forEach(function(file) { %>
                <div class="bg-gray-800 rounded-lg shadow-lg p-4">
                    <h2 class="text-lg font-bold mb-2 text-amber-400 capitalize text-center">
                        <%= file.title.replace('.txt', '') %>
                    </h2>
                    <p class="text-gray-300 mb-3">
                        <%= file.content || 'No Content' %>
                    </p>
                    <a href="/show/<%= file.title %>" class="text-amber-400 hover:underline">→ Read Full Content</a>
                    <form action="/delete/<%= file.title %>" method="post" class="mt-4 self-end">
                        <button type="submit"
                            class="text-sm bg-red-600 text-white px-3 py-1 rounded hover:bg-red-700 transition">
                            Delete
                        </button>
                    </form>
                </div>

                <% }) %>
                <% } else { %>
                <h2 class="text-center w-full col-span-3 text-gray-400">No Task Yet</h2>
                <% } %>
            </div>
        </div>





    </div>

</body>

</html>